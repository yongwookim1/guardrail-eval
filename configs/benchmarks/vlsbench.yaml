name: vlsbench
class: guardrail_eval.benchmarks.vlsbench.VLSBench

# Local dataset directory. Place `data.json` and `imgs.tar` here.
dataset_path: datasets/vlsbench

metadata_file: data.json
images_archive: imgs.tar

expected_label: unsafe
