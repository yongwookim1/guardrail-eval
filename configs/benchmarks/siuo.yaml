name: siuo
class: guardrail_eval.benchmarks.siuo.SIUO

# Local dataset directory. Place the cloned dataset repo here.
dataset_path: datasets/SIUO

# Which task file inside the repo to read. `siuo_gen.json` is open-ended
# generation; switch to the multiple-choice JSON file for the option-selection variant.
prompts_file: siuo_gen.json
images_dir: images

expected_label: unsafe
