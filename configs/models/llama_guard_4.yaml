name: llama_guard_4
class: guardrail_eval.models.llama_guard.LlamaGuard4
model_path: models/Llama-Guard-4-12B

backend: transformers
backend_kwargs:
  dtype: bfloat16
  device: cuda
  device_map: cuda

sampling:
  temperature: 0.0
  max_tokens: 20                     # output is terse: "unsafe\nS9"
