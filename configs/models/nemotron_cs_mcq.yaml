name: nemotron_cs_mcq
class: guardrail_eval.models.mcq.NemotronContentSafetyMCQModel
task_types: [mcq]
model_path: models/Nemotron-3-Content-Safety

backend_kwargs:
  dtype: bfloat16
  device: cuda
  device_map: cuda

system_prompt: |
  You are a careful multimodal reasoning assistant.
  Choose the single best answer for the multiple-choice question.
